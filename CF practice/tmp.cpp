#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
#pragma GCC optimize("Ofast")
#pragma GCC optimize("unroll-all-loops")

int main(void)
{
    int n;
    for (int i = 0; i < 1e8; i++)
        n = rand();
    return 0;
}